export const DEFAULT_DB_PORT = '27017';
export const DEFAULT_USER_PASSWORD = '123456';

export const Commands = {
  help: '--help',
  version: '--version',
  import: '--import',
  generate: '--generate',
} as const;
